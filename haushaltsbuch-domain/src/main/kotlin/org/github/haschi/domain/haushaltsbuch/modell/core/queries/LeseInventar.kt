package org.github.haschi.domain.haushaltsbuch.modell.core.queries

import org.github.haschi.domain.haushaltsbuch.modell.core.values.Aggregatkennung

data class LeseInventar(val ausInventur: Aggregatkennung)
