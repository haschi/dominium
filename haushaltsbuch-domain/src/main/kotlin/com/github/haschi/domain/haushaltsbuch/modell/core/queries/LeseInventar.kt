package com.github.haschi.domain.haushaltsbuch.modell.core.queries

import com.github.haschi.domain.haushaltsbuch.modell.core.values.Aggregatkennung

data class LeseInventar(val ausInventur: Aggregatkennung)
